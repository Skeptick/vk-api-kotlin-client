apply plugin: 'kotlin'

sourceSets {
    main.java.srcDirs += 'src/main/kotlin'
    test.java.srcDirs += 'src/test/kotlin'
}

dependencies {
    compile project(':vk-api-kotlin-client')
    compile "org.asynchttpclient:async-http-client:2.6.0"
}